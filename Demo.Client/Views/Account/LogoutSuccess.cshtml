@{
    ViewData["Title"] = "Logout effettuato";
}

<div class="container text-center mt-5">
    <h1 class="display-4 text-dark">Logout effettuato con successo!</h1>
    <p class="lead text-secondary">Arrivederci e grazie per aver utilizzato il nostro servizio.</p>
    <a href="@Url.Action("Index", "Home")" class="btn btn-primary btn-lg mt-4">Torna alla Home</a>
</div>